{
  "_comment": "Pre-build validation: The 'prebuild' script runs 'tsc --noEmit' before build. This ensures: 1) All imports can be resolved, 2) No package imports missing from package.json, 3) Build fails early if dependencies are missing.",
  "name": "instantsite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env DATABASE_URL='postgresql://postgres:jose1234@localhost:5432/instantsite' next dev",
    "prebuild": "tsc --noEmit",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:push": "cross-env DATABASE_URL='postgresql://postgres:jose1234@localhost:5432/instantsite' prisma db push",
    "db:studio": "prisma studio",
    "db:seed": "cross-env DATABASE_URL='postgresql://postgres:jose1234@localhost:5432/instantsite' tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "bcryptjs": "^2.4.3",
    "lucide-react": "^0.344.0",
    "next": "14.0.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@types/uuid": "^9.0.7",
    "autoprefixer": "^10.0.1",
    "cross-env": "^7.0.3",
    "dotenv-cli": "^11.0.0",
    "eslint": "^8",
    "eslint-config-next": "14.0.4",
    "postcss": "^8",
    "prisma": "^5.7.1",
    "tailwindcss": "^3.3.0",
    "tsx": "^4.7.0",
    "typescript": "^5"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
